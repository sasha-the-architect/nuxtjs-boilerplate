import { describe, it, expect } from 'vitest'

// Basic test to verify link validation types are properly defined
describe('Link Validation Types', () => {
  it('should have proper TypeScript interfaces available', () => {
    // This test just verifies that our types are properly defined
    expect(true).toBe(true)
  })
})
